# nginx wants root access for 80s port
# and drops privileges itself
user: root
executable-path: /usr/sbin/nginx
arguments:
- nginx
- -c # configuration file follows
- /etc/nginx/nginx.conf
- -g # let's set some config
- daemon off
